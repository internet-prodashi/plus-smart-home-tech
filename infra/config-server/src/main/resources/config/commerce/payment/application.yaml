# Hibernate
spring:
  jpa:
    hibernate:
      ddl-auto: update

  # Postgres
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:6437/payment-db
    username: dbuser
    password: '12345'

server:
  port: 0

payment:
  VAT: 0.1
